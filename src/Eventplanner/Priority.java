package Eventplanner; // Paquete para la aplicación EventPlanner.

/**
 * Enum que representa la prioridad de un evento.
 * Los valores de prioridad son: HIGH, MEDIUM, LOW.
 */
public enum Priority {
    HIGH, MEDIUM, LOW;
}
